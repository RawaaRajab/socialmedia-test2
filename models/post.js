class Post {
    constructor(id, ownerId, title, description) { //desc. = post
        this.id = id;
        this.ownerId = ownerId;
        this.title = title;
        this.description = description;
    }
};

export default Post;
//if added manually ?