class Post {
    constructor(id, ownerId, title, description) { //desc. = post
        this.id = id;
        this.ownerId = ownerId; 
        this.title = title;
        this.description = description;
    }
}


//if added manually ?